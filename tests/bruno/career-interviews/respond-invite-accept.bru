meta {
  name: Respond to invite - accept
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/career-interviews/{{interviewId}}/respond
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "action": "accepted"
  }
}

assert {
  res.status: eq 200
}