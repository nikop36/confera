meta {
  name: Register weak password
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/auth/register
  body: json
  auth: none
}

body:json {
  {
    "email": "weak@confera.com",
    "password": "weak",
    "displayName": "Weak User"
  }
}

assert {
  res.status: eq 400
}