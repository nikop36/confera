meta {
  name: Participant accepts connection request
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/connections/requests/{{connectionRequestId}}/approve
  body: none
  auth: bearer
}

auth:bearer {
  token: {{participantToken}}
}

assert {
  res.status: eq 200
  res.body.message: isDefined
}