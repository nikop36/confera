meta {
  name: Cleanup - delete interview
  type: http
  seq: 9
}

delete {
  url: {{baseUrl}}/career-interviews/{{interviewId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

assert {
  res.status: eq 200
}