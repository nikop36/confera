meta {
  name: Cleanup - organizer removes connection
  type: http
  seq: 12
}

delete {
  url: {{baseUrl}}/connections/{{connectionRequestId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{organizerToken}}
}

assert {
  res.status: eq 200
}